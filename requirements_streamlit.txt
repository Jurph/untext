# Additional dependencies for the Streamlit web interface
# Install core dependencies first: pip install -r requirements.txt
# Then install web interface dependencies: pip install -r requirements_streamlit.txt

streamlit>=1.28.0
streamlit-drawable-canvas-fix>=0.9.8  # Fixed fork compatible with Streamlit 1.45+
streamlit-image-annotation>=0.4.0  # Kept for fallback, but not used in happy path
streamlit-js-eval>=0.1.5  # For querying DOM/CSS values from JavaScript 