# Core dependencies for untextre
# Install with: pip install -r requirements.txt
#
# IMPORTANT: Install PyTorch BEFORE running this file!
# torch and torchvision are intentionally excluded here because pip will
# pull a CPU-only build from PyPI, overwriting any CUDA-enabled version.
# See README.md for PyTorch installation instructions.
#
# For web interface: pip install -r requirements_streamlit.txt
# For development:   pip install -r requirements_dev.txt

python-doctr>=0.6.0
Pillow>=9.5.0
simple-lama-inpainting==0.1.2
opencv-python>=4.8.0
numpy>=1.24.0
scikit-image>=0.21.0
easyocr>=1.7.0
